import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test placeholder', (WidgetTester tester) async {
    // Real tests require a BLE mock; this keeps the test suite runnable.
    expect(true, isTrue);
  });
}
